/* 
MOVIMENTACAO
    GET :ID_USER                        -- OBTER
    POST [
        ID_PRODUTO:
        QUANTIDADE:
        VALOR:
        ...
    ]                         -- ADICIONAR
*/
exports.GET = (req, res) => {
    if (req.query.id) {
        // TEM ID
    }else{
        // TODOS
    }
}
exports.POST = (req, res) => {
    if (req.query.id) {
        
    }
}